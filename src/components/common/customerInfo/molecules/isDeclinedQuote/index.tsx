import { COVER_CONTAINER_STYLE, COVER_SPAN_STYLE } from '../constants';

export default function IsDeclinedQuote() {
  return (
    <div className={COVER_CONTAINER_STYLE}>
      <span className={COVER_SPAN_STYLE}>반려된 요청이에요</span>
    </div>
  );
}
